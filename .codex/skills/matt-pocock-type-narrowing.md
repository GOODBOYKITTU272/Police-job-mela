# Matt Pocock Skill: Type Narrowing

- Use discriminated unions for state/result objects.
- Use `in`, `typeof`, and custom predicates for safe branching.
- Use `satisfies` to preserve literal types while validating object shape.
