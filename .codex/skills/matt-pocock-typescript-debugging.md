# Matt Pocock Skill: TypeScript Debugging

- Prefer narrowing unknown values before assertion.
- Keep API response types explicit at the boundary.
- Favor small pure helper functions for transformations.
- Avoid `any`; use unions + guards.
